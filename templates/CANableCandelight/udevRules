# CANable USB module udev rules - candlelight firmware
# for a single interface, serial number is unneeded
# for more than one, include the serial number line below and set unque names for each interface
#  ATTRS{serial}=="",\

# replace DEV with CAN port (e.g., can0)
# rename file to e.g., can0.rules


SUBSYSTEM=="net", ACTION=="add",\
  ATTRS{idVendor}=="1d50", ATTRS{idProduct}=="606f",\
  NAME="DEV",\
  RUN+="/usr/bin/logger [udev] CANable Candlelight detected DEV",\
  RUN+="/usr/local/bin/DEVadd.sh"
